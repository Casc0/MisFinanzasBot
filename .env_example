# --- Configuración General ---
# Tu zona horaria local
GENERIC_TIMEZONE=America/Argentina/Buenos_Aires

# El host donde corre n8n (localhost si es local, o tu dominio si es VPS)
N8N_HOST=localhost

# --- Conectividad Externa ---
# IMPORTANTE: Aquí va la URL pública (Ngrok, Cloudflare Tunnel o Dominio propio)
# Ejemplo: https://tunel-random.ngrok-free.dev
WEBHOOK_URL=https://example.com/

# --- Seguridad (Acceso al panel de n8n) ---
# Usuario y contraseña para entrar a editar los workflows
N8N_USER=admin
N8N_PASSWORD=changeme

# --- Credenciales de Servicios (Solo referencia) ---
# Nota: En n8n, las API Keys de Google y Telegram se configuran dentro de la UI (Credentials),
# pero puedes dejarlas aquí si planeas inyectarlas via variables de entorno en el futuro.
# TELEGRAM_BOT_TOKEN=
# GOOGLE_SHEETS_ID=
# GOOGLE_AI_API_KEY=